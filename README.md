# karadom-patches
Patch firmate (Ed25519) per KaraDom, servite via raw.githubusercontent.
NON contiene segreti: solo `manifest.json`, le patch `*.py` e le firme `*.py.sig`.
La chiave privata di firma sta solo sul PC di chi pubblica (mai qui).
